import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static export if deploying to static hosting (comment out for Vercel/Node)
  // output: "export",
};

export default nextConfig;
